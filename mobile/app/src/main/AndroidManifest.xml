<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AnniMemo">
        <activity
            android:name=".features.auth.ui.LandingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".features.auth.ui.LoginActivity" />
        <activity android:name=".features.auth.ui.RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".features.pets.ui.AddPetActivity" />
        <activity android:name=".features.pets.ui.HealthMetricsActivity" />
    </application>

</manifest>
